## 0.1.0

Initial release: sealed `GateState` (Idle/Deciding/Decided), `GateCubit` (with stale-result guard), `GateBloc` (sequential by default), `GateBlocListener` (loading overlay + navigation via the global adapter, `onDecided`, `navigateOnFallback`).
