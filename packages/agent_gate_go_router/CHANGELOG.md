## 0.1.0

Initial release: `GoRouterAdapter` (go/push/pushReplacement/replace, named routes, custom extra), `GateExtra` + `GoRouterState.gateDecision`, `GateRoute.page`, `GateRoute.redirect` / `gateRedirect`.
